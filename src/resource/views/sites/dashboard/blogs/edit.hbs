<div class="row">
  <div class="col-md-12">
    <div class="card card-outline card-info">
      <div class="card-header">
        <h3 class="card-title">
          Trang soạn thảo
        </h3>
        <ul class="header-dropdown m-r--5" style="display: inline-block; float: right;">
          <a id="thembaiviet" href="javascript:void(0)" blog-id="{{blog._id}}" class="btn bg-cyan btn-primary waves-effect m-b-15">
            LƯU THAY ĐỔI
          </a>
        </ul>
      </div>
      <!-- /.card-header -->
      <div class="card-body">
        <div class="form-group">
          <label for="name">Tên bài viết</label>
          <input type="text" class="form-control" id="name" placeholder="Tên bài viết" value="{{blog.name}}">
        </div>
        <textarea id="summernote">
          {{!-- Place <em>some</em> <u>text</u> <strong>here</strong> --}}
        </textarea>
      </div>
    </div>
  </div>
  <!-- /.col-->
</div>
<script>
  document.addEventListener("DOMContentLoaded", function () {
    $(function () {
      // Summernote
      $('#summernote').summernote()
    });
    $("#thembaiviet").on('click', function (e) {
      e.preventDefault();
      const html = $('#summernote').summernote('code');
      const name = $('#name').val();
      const body = {name, html}
      
      //post data
      const id = $(this).attr("blog-id");
      const API_URL = '/api/blogs' + id;
      const token = localStorage.getItem('auth_token');
      
      

    });

  });

</script>